{
    def swap(m) {
        a := get(m,'a');
        b := get(m,'b');
        put(m,'a',b);
        put(m,'b',a);
        result := m
    };

    def print(m) {
        puts(get(m,'a') + ' ' + get(m,'b'));
    };

    m := map();
    put(m,'a','hello');
    put(m,'b','world');

    print(m);
    swap(m);
    print(m)
}
